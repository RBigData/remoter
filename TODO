TODO:
  * capture "invisble" outputs (e.g., package loading, init.grid(), ...)
  * gotta do something about finalize()
  * remote connections?

Safety:
  * comm.stop() segfaults ?!?
  * C-c ????
  * **IMPORTANT** safety on running pbd_launch_client() before pbd_launch_servers()

Vignette:
  * make one
  * discuss bcast method


