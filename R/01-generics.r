### bigmachine
setGeneric(name="corename", 
  function(x) standardGeneric("corename"),
  package="pbdinline"
)

setGeneric(name="corename<-", 
  function(x, ...) standardGeneric("corename<-"),
  package="pbdinline"
)

setGeneric(name="corespernode", 
  function(x) standardGeneric("corespernode"),
  package="pbdinline"
)

setGeneric(name="corespernode<-", 
  function(x, ...) standardGeneric("corespernode<-"),
  package="pbdinline"
)



### pbs
setGeneric(name="name", 
  function(x) standardGeneric("name"),
  package="pbdinline"
)

setGeneric(name="name<-", 
  function(x, ...) standardGeneric("name<-"),
  package="pbdinline"
)

setGeneric(name="account", 
  function(x) standardGeneric("account"),
  package="pbdinline"
)

setGeneric(name="account<-", 
  function(x, ...) standardGeneric("account<-"),
  package="pbdinline"
)

setGeneric(name="shell", 
  function(x) standardGeneric("shell"),
  package="pbdinline"
)

setGeneric(name="shell<-", 
  function(x, ...) standardGeneric("shell<-"),
  package="pbdinline"
)

setGeneric(name="isInteractive", 
  function(x) standardGeneric("isInteractive"),
  package="pbdinline"
)

setGeneric(name="isInteractive<-", 
  function(x, ...) standardGeneric("isInteractive<-"),
  package="pbdinline"
)

setGeneric(name="walltime", 
  function(x) standardGeneric("walltime"),
  package="pbdinline"
)

setGeneric(name="walltime<-", 
  function(x, ...) standardGeneric("walltime<-"),
  package="pbdinline"
)

setGeneric(name="ncores", 
  function(x) standardGeneric("ncores"),
  package="pbdinline"
)

setGeneric(name="ncores<-", 
  function(x, ...) standardGeneric("ncores<-"),
  package="pbdinline"
)

